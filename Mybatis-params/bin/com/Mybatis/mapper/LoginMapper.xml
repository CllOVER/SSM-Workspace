<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  <mapper namespace="com.Mybatis.mapper.LoginMapper">
  
  
  
  <!-- 除ID以外的#{}中的内容，都为测试类中map对象中的key值 -->
  
  <select id="login" resultType="Login">
  SELECT id,username,password FROM login WHERE username=#{username} AND password=#{password}
  
  </select>
  
  <select id="login2" resultType="Login">
  SELECT id,username,password FROM login WHERE username=#{username} AND password=#{password}
  
  </select>
  
  
  
  
  
  
  
  
  
  
  
  
  
  
  
  
  
  
  </mapper>
  